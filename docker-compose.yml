services:
  foobar:
    build: .
    volumes:
      - ./in:/in
      - ./out:/out
    environment:
      TOOL_RUN: foobar
    # for local notebook development, bind ./src and rebuild so
    # gotap can regenerate parameters.py from the current tool.yml
    #volumes:
    #  - ./in:/in
    #  - ./out:/out
    #  - ./src:/src
    #ports:
    #  - 8888:8888
    # command: ["jupyter", "notebook", "--ip=0.0.0.0", "--no-browser", "--allow-root"]
    # advanced debug path
    #command: ["python", "run.py"]
